We need to integrate .tribe/snippets/GEMINI-*.md

goal:
- each core function has its own .ts file that works.
- .env.example configured for Vertex
- docs/GEMINI/ documentation on how to use gemini features inside nextjs project.

integrate into src/api/routers/gemini-*.ts

and test script that use src/api/routers/gemini 

also, add to src/env.js VERTEX_API_KEY=""